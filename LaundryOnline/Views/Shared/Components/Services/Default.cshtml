@model IEnumerable<LaundryOnline.Models.Service>
<ul class="sub-menu">
    @foreach (var item in Model)
    {
        <li><a asp-action="Details" asp-route-id="@item.ServiceId">@Html.DisplayFor(modelItem => item.ServiceName)</a></li>
    }
</ul>
