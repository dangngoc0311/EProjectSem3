@model IEnumerable<LaundryOnline.Models.Service>
<ul class="sub-menu">
    @foreach (var item in Model)
    {
        <li><a asp-action="Index" asp-controller="Services" asp-route-serviceId="@item.ServiceId">@Html.DisplayFor(modelItem => item.ServiceName)</a></li>
    }
</ul>
